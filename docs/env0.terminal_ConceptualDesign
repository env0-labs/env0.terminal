/emv0.terminal
├── /Config
│   ├── /Jsons
│   │   ├── BootConfig.json               # JSON configuration for the boot sequence.
│   │   ├── Devices.json                  # JSON configuration for network devices.
│   │   ├── Filesystem.json             # JSON configuration for the default filesystem.
│   │   └── UserConfig.json               # JSON configuration for user credentials.
│   ├── /Pocos
│   │   ├── BootConfig.cs                 # POCO for BootConfig (maps to BootConfig.json).
│   │   ├── Devices.cs                    # POCO for network device information (maps to Devices.json).
│   │   ├── Filesystem.cs                 # POCO for filesystem structure (maps to Filesystem_*.json).
│   │   └── FileEntry.cs                  # POCO for TODO update function
│   │   └── FileEntryConverter.cs         # POCO for TODO update function
│   │   └── UserConfig.cs                 # POCO for user configuration (maps to UserConfig.json).
│   ├── AssemblyInfo.cs                    # TODO update function
│   ├── JsonLoader.cs                      # TODO update function 
├── /Filesystem
│   ├── FilesystemManager.cs              # Manages the read-only filesystem (including file operations like cat, read).
├── /Login
│   ├── LoginHandler.cs                   # Manages local login and SSH login logic.
│   ├── LoginResultStatus.cs              # Manages login result status.
│   └── SSHHandler.cs                     # Handles SSH connections, including login and session management.
├── /Network
│   ├── NetworkManager.cs                 # Manages network discovery (nmap, ping) and SSH connections.
├── /Terminal
│   ├── CommandParser.cs                  # Command parsing (case-insensitive, filtering special chars).
│   ├── TerminalStateManager.cs           # Manages the states of the terminal (boot, login, shell, SSH stack).
