# env0.terminal (archived)

This repository has been archived. The terminal renderer is now part of the core engine in [`env0.core`](https://github.com/env0-labs/env0.core), under `core/terminal/`.

As of commit `6f38415`, all active development has moved there. This repo is read-only and no longer maintained.
